#if !defined(_CONFIG_H_)
#define _CONFIG_H_

// length of a number in binary presentation
#define _BINARY_LENGTH 128

// floating point length in binary presentation
#define _FP_LENGTH 128
// floating point exponent length
#define _FP_EXPONENT 15
// floating point mantissa length
#define _FP_MANTISSA 112

#endif // _CONFIG_H_
